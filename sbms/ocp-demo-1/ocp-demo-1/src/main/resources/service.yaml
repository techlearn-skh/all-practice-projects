apiVersion: v1
kind: Service
metadata:
  name: ocp-demo-service
  namespace: kamalredhat-2025-dev
spec:
  selector:
    app: ocp-demo
  ports:
    - protocol: TCP
      name: http
      port: 9001
      targetPort: 9001