Run this application locally and run below docker command and access prometheus application using
http://localhost:9090/query
http://host.docker.internal:9001/actuator/prometheus



--------------------------------------------------------------------------------------------------------------------
$ docker run -d -p 9090:9090 -v $(pwd)/prometheus.yml:/etc/prometheus/prometheus.yml prom/prometheus

$ docker run -d -p 9090:9090 -v /d/Practice/all-practice-projects/sbms/ocp-demo-1/ocp-demo-1/src/main/resources/prometheus.yml:/etc/prometheus/prometheus.yml prom/prometheus


Open Gitbash in respource folder and run the below command.
WORKING COMMAND:
MSYS_NO_PATHCONV=1 docker run -d -p 9090:9090 -v $(pwd)/prometheus.yml:/etc/prometheus/prometheus.yml prom/prometheus

we are adding MSYS_NO_PATHCONV=1, because mounting is not happenining if we remove this code.


$ docker exec -it 6b497dd4c62c sh
$ cat /etc/prometheus/prometheus.yml
$ docker inspect 6b497dd4c62c

--------------------------------------------------------------------------------------------------------------------
$ oc apply -f service.yaml
$ oc get svc

$ oc apply -f route.yaml
$ oc get route




--------------------------------------------------------------------------------------------------------------------