apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ocp-demo-route
  namespace: kamalredhat-2025-dev
spec:
  to:
    name: 'ocp-demo-service'
    weight: 100
    kind: Service
  port:
    targetPort: http