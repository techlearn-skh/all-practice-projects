apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "helm.fullname" . }}
spec:
  to:
    kind: Service
    name: {{ include "helm.fullname" . }}
  port:
    targetPort: http
  tls:
    termination: edge
